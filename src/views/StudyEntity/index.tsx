import EntityPage, { EntityTitle } from '@ferlab/ui/core/pages/EntityPage';

const StudyEntity = () => (
  <EntityPage links={[]} loading={false} pageId={''}>
    <EntityTitle icon={undefined}></EntityTitle>
  </EntityPage>
);

export default StudyEntity;
